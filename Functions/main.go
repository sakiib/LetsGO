package main
 
import (
	"fmt"
	"strings"
)

// func function_name (argument) return type
func isVowel(ch string) bool {
    if ch == "a" || ch == "e" || ch == "i" || ch == "o" || ch == "u" || ch == "y" {
        return true
    } else {
        return false
    }
}
 
func main() {
	  
}
